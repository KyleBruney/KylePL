<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Content is rendered by Jetpack Compose; -->
    <!-- this theme only sets the window background before Compose draws. -->
    <style name="Theme.KylePL" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/charcoal</item>
        <item name="android:windowBackground">@color/charcoal</item>
    </style>
</resources>
